import { OnlyStringDirective } from './only-string.directive';

describe('OnlyStringDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyStringDirective();
    expect(directive).toBeTruthy();
  });
});
