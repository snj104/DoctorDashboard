import { IframeDirective } from './iframe.directive';

describe('IframeDirective', () => {
  it('should create an instance', () => {
    const directive = new IframeDirective();
    expect(directive).toBeTruthy();
  });
});
